# Rust's output directory
target

# Local settings
.soroban
.stellar

# Deployment secrets and per-network deployment records
.env.deploy
deployments/

# Soroban test snapshots: derived artifacts, regenerated by `cargo test`.
# Most are not committed because their diffs dwarf source changes.
test_snapshots/
!contracts/compatibility/test_snapshots/
!contracts/compatibility/test_snapshots/*.json
