[package]
name = "boundless-storage-compatibility"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
doctest = false

[dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
